### SDKの初期化

`Client` クラスを初期化します。 `USER_NAME` と `API_KEY` は、blastengineのダッシュボードから取得できます。

```python
Blastengine('USER_NAME', 'API_KEY')
```

- USER_NAME  
	blastengineのダッシュボードにログインするためのユーザー名です。
- API_KEY
	blastengineのダッシュボードから取得できるAPIキーです。
